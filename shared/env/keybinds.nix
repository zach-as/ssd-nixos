{ pkgs, ... }:

{
  /*
    services.keyd = {
      enable = true;
      keyboards = {
        keybinds = {
          ids = [ "*" ]; # select all keyboards
          settings = {
            # The main keyboard layer
            main = {
              capslock = "overload(meta, esc)"; # On tap, treat capslock as escape
            };
            otherlayer = { }; # define secondary layers
          };
          extraConfig = ''
            # Define any extra config here
          '';
        };
      };
    };
  */
}
